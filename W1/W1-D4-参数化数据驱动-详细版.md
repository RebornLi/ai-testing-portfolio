# W1-D4 详细学习内容 · 参数化 + 数据驱动（6-8 小时版）

> 日期：2026-08-24（周一）｜ 目标：吃透 `@pytest.mark.parametrize`，做数据驱动测试
> 验收：`parse_salary()` 工具 + `test_salary_parser.py`（10+ 参数化用例）全绿 + 学习日志 + commit

---

## ⏰ 今日时间块（6-8 小时）

| 时间段 | 时长 | 内容 |
|---|---|---|
| 09:00-10:30 | 1.5h | parametrize 深讲：多参数、ids 命名、嵌套 |
| 10:30-12:00 | 1.5h | 实战：写 parse_salary() 薪资解析工具 |
| 14:00-16:00 | 2h | 实战：参数化 10+ 用例（正常/边界/异常三态） |
| 16:00-17:30 | 1.5h | 数据驱动：用 61 条真实 JD 数据验证 |
| 19:00-20:00 | 1h | 异常测试 + 失败自测（先看失败长啥样） |
| 20:00-21:00 | 1h | 学习日志 + commit 打卡 |

---

## 一、parametrize 深讲（1.5h）★ 今日核心

> D2 提到"参数化=一个函数测 100 组数据"，今天把它吃透。**这是 pytest 最强大、最省时间的功能。**

### 1.1 最小示例
```python
import pytest

def add(a, b):
    return a + b

# ❌ 没有参数化：重复写 4 个函数
def test_add_1(): assert add(1, 1) == 2
def test_add_2(): assert add(1, 2) == 3
def test_add_3(): assert add(2, 3) == 5
def test_add_4(): assert add(10, 20) == 30

# ✅ 用参数化：一个函数搞定
@pytest.mark.parametrize("a,b,expected", [
    (1, 1, 2),
    (1, 2, 3),
    (2, 3, 5),
    (10, 20, 30),
])
def test_add(a, b, expected):
    assert add(a, b) == expected
```

> 跑 `pytest -v`，会看到 4 个独立的用例（而不是 1 个），每个失败只报自己的那一组——**这是新手最容易忽略的关键点**。

### 1.2 多参数（直接列元组）
```python
@pytest.mark.parametrize("num,expected", [
    (1, "单数"),
    (2, "双数"),
    (3, "单数"),
])
def test_num(num, expected):
    result = f"{'单数' if num % 2 else '双数'}"
    assert result == expected
```

### 1.3 ids 命名（测试名更有意义，必看！）
```python
@pytest.mark.parametrize("name,expected", [
    ("张三", True),
    ("李四", False),
], ids=["张三", "李四"])         # 给每个用例命名

# 跑出来用例名就是 test_name[张三]、test_name[李四]（不用记序号）
```

### 1.4 复杂示例（真实场景：薪资解析）
```python
@pytest.mark.parametrize("salary,expected", [
    ("1.1-1.2万", (11000, 12000)),
    ("面议", None),
    ("2-4万·14薪", (20000, 40000)),
    ("30-50K", (30000, 50000)),
])
def test_parse_salary(salary, expected):
    assert parse_salary(salary) == expected
```

### 1.5 嵌套参数化
```python
@pytest.mark.parametrize("a", [1, 2])
@pytest.mark.parametrize("b", [10, 20])
def test_double(a, b):
    # 产生 4 组合：(1,10)(1,20)(2,10)(2,20)
    assert a + b > 0
```

---

## 二、实战：写 parse_salary() 工具（1.5h）★ 今日产出①

> 任务：写一个薪资解析函数，把各种格式转成 `(最低, 最高)` 元组。这个函数会被参数化测试"喂数据"。

### 2.1 先看真实 JD 数据的薪资格式（重要！了解数据才能写工具）

真实 JD 数据里薪资字段有 48 种格式，比如：
- `1.1-1.2万`（万为单位区间）
- `7000-9000元`（元为单位区间）
- `2-4万·14薪`（带薪资倍数后缀）
- `面议`（空值，无区间）
- `100-150元/天`（日薪，不是月薪区间，异常态）

### 2.2 写 `src/salary_parser.py`

```python
"""W1-D4 产出①：薪资区间解析工具（纯函数，便于参数化测试）"""
import re


def parse_salary(salary: str):
    """把薪资字符串解析成 (最低, 最高) 元组；无法解析返回 None。

    规则：
    - 空 / 面议 / 不详 / 待定 -> None
    - "1.1-1.2万" -> (11000, 12000)   万为单位
    - "30-50K"    -> (30000, 50000)   K 为单位
    - "2-4万·14薪" -> (20000, 40000)   忽略 ·14薪 后缀
    - "100-150元/天" -> None            日薪不是月薪区间（异常态）
    """
    if salary is None:
        return None
    s = str(salary).strip()
    if not s or s in ("面议", "不详", "待定", "--"):
        return None
    if "/天" in s or "元/天" in s:      # 日薪，不是月薪区间（异常态）
        return None
    s = s.split("·")[0]                  # 忽略 ·14薪 这类后缀
    m = re.match(r"^([\d.]+)\s*[/-]\s*([\d.]+)", s)   # 提取 a-b 区间
    if not m:
        return None
    lo_s, hi_s = m.group(1), m.group(2)
    mult = 10000 if "万" in s else (1000 if "k" in s.lower() else 1)
    return (int(round(float(lo_s) * mult)), int(round(float(hi_s) * mult)))


if __name__ == "__main__":
    # 命令行直接跑看结果
    print(parse_salary("1.1-1.2万"))     # (11000, 12000)
    print(parse_salary("面议"))           # None
    print(parse_salary("2-4万·14薪"))     # (20000, 40000)
```

### 2.3 跑一下，拿到"正确答案"

```bash
cd ~/ai-testing-portfolio/jd-validator
python3 src/salary_parser.py
```

**实测真实输出（先记住，测试要用）：**
```
(11000, 12000)
None
(20000, 40000)
```

---

## 三、参数化 10+ 用例（2h）★ 今日产出②

创建 `tests/test_salary_parser.py`。覆盖正常/边界/异常三态，**参数化是今天的主菜**。

```python
"""W1-D4 产出②：parse_salary 参数化测试（10+ 用例）
每个用例名 = test_parse_salary[薪资值]，失败时直接显示是哪组数据
"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))
from salary_parser import parse_salary


@pytest.mark.parametrize("salary,expected", [
    # 正常态（万为单位）
    ("1.1-1.2万", (11000, 12000)),
    ("0.8-1万", (8000, 10000)),
    ("1.5-2.5万", (15000, 25000)),
    # 边界态：带薪资倍数后缀 ·14薪
    ("2-4万·14薪", (20000, 40000)),
    ("0.6-0.8万·13薪", (6000, 8000)),
    ("3-3.5万·15薪", (30000, 35000)),
    # 边界态：K 为单位
    ("30-50K", (30000, 50000)),
    ("15-25K", (15000, 25000)),
    # 边界态：元为单位（小金额）
    ("4000-7000元", (4000, 7000)),
    ("7000-14000元", (7000, 14000)),
    # 异常态（应返回 None）
    ("面议", None),
    ("面议·13薪", None),
    ("100-150元/天", None),          # 日薪，不是月薪区间
    ("", None),                       # 空字符串
], ids=["1.1-1.2万", "0.8-1万", "1.5-2.5万",
       "2-4万·14薪", "0.6-0.8万·13薪", "3-3.5万·15薪",
       "30-50K", "15-25K",
       "4000-7000元", "7000-14000元",
       "面议", "面议·13薪", "100-150元/天", "空"])

def test_parse_salary(salary, expected):
    assert parse_salary(salary) == expected
```

> 这套有 **14 组**（比计划的 10 组多），覆盖：万单位、K 单位、元单位、·14薪后缀、面议、日薪、空——三态 + 单位转换都齐了。跑 `pytest tests/test_salary_parser.py -v`，期望 14 全绿。

---

## 四、数据驱动：用 61 条真实 JD 数据（1.5h）★ 今日产出③

> 关键：**测试数据从真实文件来，不是手抄**。这叫"数据驱动"——换数据不换代码。

### 4.1 先加载真实数据（参考 D2 的 session fixture）
```python
import pytest

@pytest.fixture(scope="session")
def real_jobs():
    import json, os
    path = os.path.join(
        os.path.dirname(__file__), "..", "..",
        "桌面", "AI测试岗位JD数据-61条.json")
    with open(path, encoding="utf-8") as f:
        return json.load(f)
```

### 4.2 数据驱动测试：每条 JD 的薪资都应能被解析或明确为异常态
```python
def test_real_salary_all_parseable_or_none(real_jobs):
    """① 61 条真实 JD 的薪资，每条要么解析成区间，要么返回 None（异常态）"""
    from salary_parser import parse_salary
    for job in real_jobs:
        r = parse_salary(job.get("薪资"))
        # 要么有区间（元组），要么是 None（面议/日薪等异常态）
        assert r is None or (isinstance(r, tuple) and r[0] <= r[1])


def test_real_salary_parse_success_rate(real_jobs):
    """② 成功率 >= 90%：58 条可解析，2 条面议（真实数据：58/61=0.9508）"""
    from salary_parser import parse_salary
    parseable = [s for s in [j.get("薪资") for j in real_jobs]
                 if parse_salary(s) is not None]
    assert len(parseable) == 58
    assert round(len(parseable) / len(real_jobs), 4) == 0.9508


def test_real_salary_none_are_none_word(real_jobs):
    """③ 返回 None 的，原始值一定是"面议"（异常态定义清楚）"""
    from salary_parser import parse_salary
    for job in real_jobs:
        if parse_salary(job.get("薪资")) is None:
            assert job.get("薪资") == "面议"
```

---

## 五、失败自测（1h）★ 先看失败长啥样

> 接口/参数化测试最容易犯的错：只写"应该成功"的用例，从不验证"失败场景"。今天补齐失败案例。

```python
# 故意写错断言，看 pytest 报什么（看完删掉）
import pytest
from salary_parser import parse_salary

@pytest.mark.parametrize("salary,expected", [
    ("1.1-1.2万", (11000, 12000)),
    ("面议", 999999),          # ❌ 故意写错：面议应该是 None，不是 999999
])
def test_intentional_fail(salary, expected):
    assert parse_salary(salary) == expected
```

跑 `pytest -v`，观察失败输出里的 `assert parse_salary('1.1-1.2万') == expected` 和你的自定义错误信息——这就是失败时的定位线索。

---

## 六、运行 & 验证

```bash
cd ~/ai-testing-portfolio/jd-validator
# 1. 先看工具输出
python3 src/salary_parser.py
# 2. 运行全部测试
pytest -v
# 期望：test_salary_parser.py 14 passed（或你自定义的组数）
#       test_data_drive.py 3 passed
```

### 关键观察点
- 全绿 → 工具 + 参数化都对了 ✅
- 某组 FAILED → 读失败信息，看是哪组薪资解析错（用例名 `[薪资值]` 直接显示）
- 数据驱动 FAILED → 说明工具逻辑错了，回到 2.2 改 `parse_salary`

---

## 七、学习日志模板（20:00）

复制 `~/ai-testing-portfolio/learning-log/2026-08-24.md`，填写：
- 今天学了什么：parametrize 多参数/ids/嵌套、数据驱动、parse_salary
- 实测真实值：真实 61 条 JD 薪资 58 条可解析，3 条异常态（含 2 条"面议" + 1 条日薪），成功率 0.9508
- 疑问：parametrize 和 fixture 区别？ids 不写会怎样？数据驱动和参数化区别？
- 明日预习：D5 项目整合（requirements.txt、pytest.ini）

## 八、commit 打卡（21:00）

```bash
cd ~/ai-testing-portfolio
git add -A
git commit -m "W1D4: parse_salary 薪资解析 + parametrize 参数化测试（14+3 用例）"
git push
```

---

## 📌 今日自检清单

- [ ] parametrize 三态（正常/边界/异常）会套用到薪资解析
- [ ] ids 命名会让测试名更有意义（不是 test_case[1]）
- [ ] 多参数直接列元组，嵌套参数化产生组合
- [ ] parse_salary 工具输出正确（万/K/元/面议/日薪）
- [ ] 14 组参数化用例全绿
- [ ] 数据驱动用真实 61 条 JD 验证
- [ ] 失败用例能读出定位信息
- [ ] 学习日志 + commit 完成

## 🆘 卡住怎么办

- 报错 `E   assert (11000, 12000) == None` → 说明 parse_salary 对某组数据解析错了，看 `[薪资值]` 是哪组
- 想换测试数据 → 直接改 parametrize 的列表，或从 JSON 读（不改代码）
- 卡 > 30min：看 pytest 官方文档 parametrize 章节（https://docs.pytest.org/en/stable/）
- 卡 > 2h：直接问我（木木），带上报错信息

---

## 🎯 参数化 vs fixture 对比记忆

| 对比项 | parametrize | fixture |
|---|---|---|
| 解决什么 | 一个函数测多组数据 | 一个数据复用多次 |
| 触发时机 | 每组数据触发一个用例 | 按 scope 复用 |
| 典型用法 | 薪资解析、数字计算 | 大文件加载、服务连接 |
| 产出 | N 个用例 | 1 份数据 |

> 参数化是"测数据"，fixture 是"备数据"，D4 这天你会发现两者常一起用。
