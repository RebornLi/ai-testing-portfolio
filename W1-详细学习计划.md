# W1 详细学习计划 · Python 工程化 + 仓库启动

> 时间：2026-08-21（周五）~ 2026-08-27（周四）
> 目标：pytest 工程化实战 + 作品集仓库第一周可演示成果
> 验收：pytest 用例 ≥10 全绿、每天学习日志 + commit、周末可演示成果

---

## 🎯 本周总目标

1. **pytest 工程化**：fixture / mock / 参数化 / conftest / 数据驱动，全部实战会用
2. **接口测试实战**：用本地真实服务（vLLM :8000 + embedding :8081）做测试对象——**这是别人没有的练习环境**
3. **仓库成型**：`ai-testing-portfolio` 有第一个完整可演示项目
4. **简历素材**：GitHub 一周活跃记录 + 第一个 commit 链

---

## 📅 每日任务

### D1（08-21 周五）：环境 + pytest 基础
**学习内容**
- 确认 Python 3.12 环境；安装 `pytest requests`（pip）
- pytest 核心：断言（assert/approx/raises）、fixture 基础、测试发现规则、`-v/-k/-x` 运行参数
- 文件结构：`tests/` 目录约定、pytest.ini 配置

**实战练习**
- 对 61 条 JD 数据（`~/桌面/AI测试岗位JD数据-61条.json`）写 **5 个数据校验测试**：
  - ① 所有条目含非空"职位"字段
  - ② 薪资字段格式合法（含数字或"面议"）
  - ③ AI 相关条目占比 ≥ 60%（当前 45/61=73.8%）
  - ④ 城市字段在已知城市列表中
  - ⑤ 无重复条目（职位+公司+薪资 唯一）

**产出**：`ai-testing-portfolio/jd-validator/tests/test_jd_data.py` + 学习日志
**验收**：pytest 5 用例全绿

### D2（08-22 周六）：requests 接口测试实战
**学习内容**
- requests 库：get/post/headers/timeout/json() 解析、状态码断言
- 接口测试设计：正常/边界/异常三态

**实战练习**（本地真实服务！）
- 对本地 vLLM（`http://127.0.0.1:8000`）写接口测试：
  - ① `/health` 返回 200
  - ② `/v1/models` 返回模型列表且含 `nemotron3`
  - ③ 非法路径返回 404
  - ④ 无 API key 调用 `/v1/chat/completions` 返回 401
- 对 embedding 服务（`127.0.0.1:8081`）同样写 3 个用例

**产出**：`ai-testing-portfolio/jd-validator/tests/test_local_api.py`
**验收**：8 个接口用例全绿（服务已启动时）；服务未启动时用 pytest.skip 处理

### D3（08-23 周日）：fixture + mock 进阶
**学习内容**
- fixture：作用域（function/module/session）、conftest.py 共享、yield fixture（setup/teardown）
- mock：monkeypatch、unittest.mock（patch 外部调用）

**实战练习**
- 写一个"JD 分析工具"`jd_stats.py`（统计 AI 相关占比/城市分布/薪资区间），并用 fixture 传入测试数据
- 用 mock 模拟 LLM API 响应（不真正调 :8000），测试一个"LLM 响应解析函数"（解析 choices[0].message.content）
- fixture 实战：session 级 fixture 只加载一次 JD 数据（性能优化）

**产出**：`jd-validator/src/jd_stats.py` + `tests/test_jd_stats.py` + `tests/test_llm_parser.py`
**验收**：新增 5+ 用例全绿

### D4（08-24 周一）：参数化 + 数据驱动
**学习内容**
- `@pytest.mark.parametrize` 多参数组合、ids 命名
- 数据驱动：从 JSON/CSV 读测试数据
- 异常测试：`pytest.raises`

**实战练习**
- 薪资解析函数 `parse_salary()`（"1.1-1.2万"→(11000,12000)；"面议"→None；"2-4万·14薪"→(20000,40000)），参数化 10+ 组输入（正常/边界/异常）
- 用 61 条 JD 真实数据做数据驱动测试（每条数据的薪资都能被解析或合法）

**产出**：`tests/test_salary_parser.py`（参数化 10+ 用例）
**验收**：全绿；能解释参数化的价值（一个函数测 100 组数据）

### D5（08-25 周二）：项目整合 + 结构化
**学习内容**
- 项目结构规范：`src/ + tests/ + docs/`、requirements.txt、pytest.ini、.gitignore
- 命令行工具：argparse 或 click 做一个 `jd-report` 命令（输出统计报告）

**实战练习**
- 整合 D1-D4 所有测试进 `jd-validator` 项目
- 补 `requirements.txt`、`pytest.ini`（配置 testpaths/addopts）
- `jd-report` 命令输出：总条数、AI 占比、城市 TOP5、薪资分布（输出到 reports/）

**产出**：完整项目结构 + `reports/w1_jd_report.md`
**验收**：`pytest` 一键全绿；`python -m jd_validator.report` 能出报告

### D6（08-26 周三）：代码质量 + README
**学习内容**
- 代码规范：函数注释、类型提示（可选）、命名
- README 写作规范（项目介绍/安装/运行/测试/结果）

**实战练习**
- 写 `jd-validator/README.md`：项目简介 + 截图（pytest 运行结果）+ 测试报告链接
- 全量检查：pytest 全绿、代码整洁、无调试残留
- 学习日志补齐本周疑问复盘

**产出**：README + 最终代码审查
**验收**：陌生人能按 README 5 分钟跑通项目

### D7（08-27 周四）：里程碑收尾 + 复盘
**任务**
- 写 `milestones/W1-Python工程化.md`（本周成果/运行方式/复盘）
- 全部 push 到 GitHub（`git add -A && commit && push`）
- 复盘：完成度自评、卡点、W2 预习（LLM API 参数文档）

**验收**：GitHub 仓库一周 7+ commits，含 1 个完整可演示项目

---

## 📦 本周产出物清单

| 产出 | 位置 | 验收 |
|---|---|---|
| JD 数据校验测试 | `jd-validator/tests/test_jd_data.py` | 5 用例绿 |
| 本地 API 接口测试 | `jd-validator/tests/test_local_api.py` | 8 用例绿 |
| JD 统计分析工具 | `jd-validator/src/jd_stats.py` | 输出正确 |
| LLM 响应解析 + mock | `jd-validator/tests/test_llm_parser.py` | mock 不依赖真服务 |
| 薪资解析参数化 | `jd-validator/tests/test_salary_parser.py` | 10+ 用例绿 |
| 统计报告 | `jd-validator/reports/w1_jd_report.md` | 真实数据 |
| 项目 README | `jd-validator/README.md` | 可复现 |
| 周里程碑 | `milestones/W1-Python工程化.md` | 可演示 |
| 学习日志 ×7 | `learning-log/2026-08-2*.md` | 每日一篇 |

---

## 🛠️ 本地环境（本周要用）

| 服务 | 地址 | 用途 |
|---|---|---|
| vLLM 对话模型（nemotron3） | `http://127.0.0.1:8000` | 接口测试对象 + 后续 LLM 测试 |
| vLLM 嵌入模型 | `http://127.0.0.1:8081` | 接口测试对象 + W3 RAG 用 |
| API Key | 在 openclaw.json 里，测试脚本从环境变量读 | 不要硬编码！ |

⚠️ 接口测试前先确认服务状态：`curl http://127.0.0.1:8000/health`

---

## 🚀 验收清单（周日 08-27 检查）

- [ ] `pytest` 全量运行：0 失败 0 跳过（跳过仅限服务未启动场景）
- [ ] GitHub 仓库 7+ commits（每天至少 1 个）
- [ ] 每天学习日志已写（含疑问）
- [ ] `jd-validator/README.md` 可复现
- [ ] 周里程碑已推送

## ⏰ 时间建议

- 每天 3-5 小时：2h 学习 + 1-2h 实战 + 0.5h 日志/commit
- 卡住超过 2 小时 → 问我（木木），别硬扛
- 周末（D2-D3）多投入，平时见缝插针
