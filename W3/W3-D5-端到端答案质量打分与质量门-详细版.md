# W3-D5 详细学习内容 · 端到端答案质量打分与质量门（6-8 小时版）

> 日期：2026-09-09（周二）｜ 目标：评估答案本身"答到位了吗"——相关性 + 综合质量门
> 验收：`test_rag_quality.py` 11 用例全绿 + 学习日志 + commit

---

## ⏰ 今日时间块（6-8 小时）

| 时间段 | 时长 | 内容 |
|---|---|---|
| 09:00-10:30 | 1.5h | 为什么答案质量也要量化（D3/D4 只测了检索和幻觉） |
| 10:30-12:00 | 1.5h | 答案相关性：覆盖多少问题关键词 |
| 14:00-16:00 | 2h | 实战：`quality.py`（相关性 + 完整性 + 质量门） |
| 16:00-17:30 | 1.5h | 综合分 = 相关性×w1 + 忠实度×w2，定阈值卡质量 |
| 19:00-20:30 | 1.5h | 端到端质量门 + StaticAnswerer 解耦真实模型 |
| 20:30-21:00 | 0.5h | 学习日志 + commit 打卡 |

---

## 一、为什么答案质量也要量化（1.5h）★ 今日重点

> 到 D5 为止，你已经能回答三个问题：
> 1. **检索**找得准不准？ → D3（Recall/HitRate）
> 2. **答案**有没有编造？ → D4（接地分）
>
> 今天补第四个：
> 3. **答案**有没有答到点上？ → 答案相关性 + 综合质量门

> 关键洞察：**"相关"和"忠实"是两件事。**
> - 一个答案可能答得跑题（相关性低）但没编造（忠实度高）
> - 也可能答得很相关但夹带了上下文没有的信息（相关性高但忠实度低）
>
> 所以最终要**两个都看**，合成一个综合分。

### 1.1 RAG 评估的四件事（本周小结）
| 维度 | 指标 | 哪天 |
|---|---|---|
| 检索找得准 | Recall@K / HitRate | D3 |
| 答案有没有编造 | 接地分 / 幻觉 | D4 |
| 答案答没答到点 | 答案相关性 | **D5** |
| 综合判合格 | 综合质量门 | **D5** |

---

## 二、答案相关性（1.5h）

> 最朴素的度量：**答案覆盖了问题里多少关键词。**

```python
answer_relevance(answer, question) = len(答案词 ∩ 问题词) / len(问题词)
```

> 为什么分母是**问题**（不是答案）？
> 因为相关性关注"有没有答到问题"。答得再长，没覆盖问题关键词 = 跑题。

> ⚠️ **认知边界**：
> - 真实答案质量靠 **LLM-as-Judge**（让 LLM 给答案打分）或 **embedding 语义相似度**。
> - 关键词重叠能可靠检测"答非所问 / 漏答关键词"，但认不出"同义改写"。
> - 这是教学替身，留待后续接入真实模型升级。

---

## 三、实战：`quality.py`（2h）★ 今日产出①

```python
"""quality.py — RAG 答案质量打分（端到端问答）

认知边界：答案相关性用"答案覆盖问题关键词的比例"近似，
离线可复现。能检测"答非所问 / 漏答"，无法识别同义改写。
"""
import re
from grounding import ground_claims, grounding_score


def answer_relevance(answer, question):
    """答案覆盖问题关键词的比例。分母 = 问题词数。"""
    q = _keywords(question)
    a = _keywords(answer)
    if not q:
        return 0.0
    return len(a & q) / len(q)


def answer_relevance_breakdown(answer, question):
    """返回相关性 + 命中的问题词集合，便于调试。"""
    q = _keywords(question)
    a = _keywords(answer)
    if not q:
        return 0.0, set()
    hit = q & a
    return len(hit) / len(q), hit


def composite_score(answer, question, context, weight_rel=0.5,
                    weight_faith=0.5, threshold=0.6):
    """综合质量分 = 相关 × w1 + 忠实 × w2。

    返回 (score, passed)：
      score = 0~1（保留 4 位小数），passed = score >= threshold。
    """
    rel = answer_relevance(answer, question)
    faith = grounding_score(answer, context)
    score = weight_rel * rel + weight_faith * faith
    return round(score, 4), score >= threshold


class QualityGate:
    """质量门：给定阈值，判定答案合格不合格。

    用法：
        gate = QualityGate(threshold=0.6)
        gate.is_pass(answer, question, context)   # bool
        gate.score(answer, question, context)      # float
    """

    def __init__(self, threshold=0.6, weight_rel=0.5, weight_faith=0.5):
        self.threshold = threshold
        self.weight_rel = weight_rel
        self.weight_faith = weight_faith

    def score(self, answer, question, context):
        _, s = composite_score(answer, question, context,
                               self.weight_rel, self.weight_faith,
                               self.threshold)
        return s

    def is_pass(self, answer, question, context):
        _, passed = composite_score(answer, question, context,
                                    self.weight_rel, self.weight_faith,
                                    self.threshold)
        return passed


class StaticAnswerer:
    """固定答案的答案器（测试用，绕开真实 LLM）。"""

    def answer(self, question, contexts):
        """默认：把检索到的上下文直接拼成答案（最朴素答案器）。"""
        return "".join(c for c in contexts)
```

---

## 四、测试设计（产出②）★ 11 用例全绿

> 全部离线、可复现，不依赖真实模型。三组关注点：

### 4.1 答案相关性（3 用例）
```python
def test_answer_relevance_full():
    """① 答案完整覆盖问题关键词 → 相关性 = 1.0。"""
    assert answer_relevance("向量数据库是什么库", "什么是向量数据库") == 1.0

def test_answer_relevance_partial():
    """② 答案只覆盖部分关键词 → 0 < 相关性 < 1。"""
    r = answer_relevance("向量数据库用于存储文本向量", "什么是向量数据库")
    assert 0.0 < r < 1.0

def test_answer_relevance_unrelated():
    """③ 答案与问题无关 → 相关性接近 0。"""
    r = answer_relevance("圆周率约等于3.14159和黄金比例", "今天中午吃什么")
    assert r == pytest.approx(0.0)
```

### 4.2 综合质量门（3 用例）
```python
def test_composite_full_answer_passes():
    """⑦ 相关且接地的答案 → 综合分高，质量门通过。"""
    score, passed = composite_score("向量数据库是什么", QUESTION, CONTEXT, 0.6)
    assert score >= 0.6
    assert passed is True

def test_composite_fabricated_fails():
    """⑧ 答非所问的答案 → 综合分低，质量门拒绝。"""
    score, passed = composite_score(
        "圆周率约等于3.14159和黄金比例常数", QUESTION, CONTEXT, 0.6)
    assert score < 0.6
    assert passed is False

def test_composite_score_in_range():
    """⑨ 综合分永远落在 [0, 1]。"""
    for ans in ["向量数据库", "圆周率3.14", "无关句子无关"]:
        score, _ = composite_score(ans, QUESTION, CONTEXT, 0.6)
        assert 0.0 <= score <= 1.0
```

### 4.3 质量门对象 + 接口契约（3 用例）
```python
def test_quality_gate_object():
    """⑩ QualityGate 封装 score + is_pass，判定一致。"""
    gate = QualityGate(threshold=0.6)
    score = gate.score("向量数据库用于存储向量", QUESTION, CONTEXT)
    assert 0.0 <= score <= 1.0
    _, comp_pass = composite_score("向量数据库是什么", QUESTION, CONTEXT, 0.6)
    assert gate.is_pass("向量数据库是什么", QUESTION, CONTEXT) == comp_pass

def test_static_answerer_interface():
    """⑪ StaticAnswerer.answer(question, contexts) 返回拼接上下文。"""
    assert StaticAnswerer().answer("q", ["向量", "数据库"]) == "向量数据库"
```

> 完整 11 用例在 `test_rag_quality.py`，覆盖 ①-⑪。

---

## 五、端到端质量门（1.5h）

> 用质量门把"答案质量"变成一个开关：合格 / 不合格。
> 生产里可以用它卡流水线——低于阈值就重试或转人工。

```python
def evaluate_end_to_end(golden, answer_fn, threshold=0.6):
    """端到端质量评估：对每个 (query, context) 算质量分，返回通过率。

    answer_fn: callable(question, contexts) -> answer 字符串。
    golden: list[(query, context)]
    """
    passed = 0
    for question, context in golden:
        answer = answer_fn(question, context)
        _, ok = composite_score(answer, question, context, threshold)
        if ok:
            passed += 1
    return passed / len(golden)
```

> 关键：**质量门逻辑在 `quality.composite_score`**，答案器是任意实现了
> `answer(question, contexts)` 的对象。测试里用 `StaticAnswerer`，
> 生产里换成真实 LLM 答案器即可——质量门逻辑不动。

---

## 六、失败自测（1h）

```python
import pytest

def test_intentional_fail():
    # 把"答非所问的答案相关性低"错写成"=1.0"，看 pytest 报什么
    r = answer_relevance("圆周率约等于3.14159", "今天中午吃什么")
    assert r == pytest.approx(1.0)  # 实测 0.0
```

---

## 七、运行 & 验证

```bash
cd ~/ai-testing-portfolio/rag-demo
pytest test_rag_quality.py -v
# 期望: 11 passed
```

### 关键观察点
- 全绿 → 相关性、完整性、综合分、质量门都对了 ✅
- ③ 相关性 = 0 → 确认"无关答案确实一个关键词都没覆盖"
- ⑧ 综合分 < 0.6 → 确认"跑题答案过不了门"

---

## 八、学习日志模板（20:30）

复制 `~/ai-testing-portfolio/learning-log/2026-09-09.md`，填写：
- 今日学了什么：答案相关性 = 覆盖多少问题关键词；综合分 = 相关性×w + 忠实度×(1-w)；质量门卡合格线
- 卡点：为什么分母用问题词数而不是答案词数？
- 明日预习：W3-D6（端到端评估报告 + 全周里程碑）

## 九、commit 打卡（21:00）

```bash
cd ~/ai-testing-portfolio
git add -A
git commit -m "W3D5: 端到端答案质量打分 + 综合质量门（11 测试全绿）"
git push
```

---

## 📌 今日自检清单

- [ ] 懂"相关"和"忠实"是两件事
- [ ] 会算 answer_relevance
- [ ] 会写 composite_score（加权合成）
- [ ] 会写 QualityGate
- [ ] 质量门阈值能卡住跑题答案
- [ ] 日志 + commit 完成

## 🆘 卡住怎么办

- 相关性算不准 → 确认分母是问题词数
- 质量门太严/太松 → 调 `threshold`，先跑一批 golden 看分数分布
- 卡 > 30min → 想清楚"相关性 = 答案覆盖多少问题关键词"
- 卡 > 2h → 问木木，带报错信息

---

## 🎯 今日关键词

```
answer_relevance   → 答案覆盖多少问题关键词（分母=问题）
answer_completeness → 答案覆盖了问题多少内容
composite_score    → 相关×w1 + 忠实×w2（0~1）
QualityGate        → 阈值判定合格/不合格
StaticAnswerer     → 测试用的固定答案答案器
threshold          → 质量门阈值（默认 0.6）
```

> 今天的核心：**没有答案质量的闸门，RAG 系统就无法在生产里上线。**
> 相关性 + 忠实度 = 一个分数，一个阈值 = 一道防线。

---

## 🎯 W3 全周预览

| 日 | 主题 | 核心 |
|---|---|---|
| D1 | RAG 入门 | 分块 + 确定性嵌入 + 余弦检索 |
| D2 | 真实 Embedder | 工厂模式 + 集成测试 |
| D3 | 检索质量 | Recall/Precision/MRR/HitRate |
| D4 | 幻觉检测 | 确定性接地打分 |
| D5 | 答案质量门 | 相关性 + 综合分（今天） |
| D6 | 端到端报告 | 全流程评估 + 报告 |
| D7 | 里程碑 | 可演示成果 + push |
