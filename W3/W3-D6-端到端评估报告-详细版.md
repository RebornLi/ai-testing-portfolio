# W3-D6 详细学习内容 · 端到端评估报告（6-8 小时版）

> 日期：2026-08-24（周日）｜ 主题：端到端 RAG 评估报告
> 目标：把 D1-D5 的检索质量、幻觉检测、答案质量三条线整合成一份端到端报告
> 产出：`rag/report.py`（评估引擎 + 报告格式化）、`test_rag_report.py`（9 用例全绿）

---

## 今日产出

| 文件 | 内容 | 行数 |
|---|---|---|
| `rag-demo/rag/report.py` | RagEvaluator 评估引擎 + 报告格式化 | 118 |
| `rag-demo/test_rag_report.py` | 端到端评估测试（9 用例） | 156 |

---

## 核心设计

### 1. 为什么需要端到端评估

D1-D5 分别验证了：
- **检索质量**：Recall@K、Precision@K、HitRate
- **幻觉检测**：接地分数（deterministic grounding）
- **答案质量**：答案相关性、答案完整性

但这些都还比较孤立。一个真实的 RAG 系统，我们关心的是**整条流水线跑下来的综合质量**。D6 把这三条线整合成一份报告。

### 2. 报告格式

```
================================================
RAG 端到端评估报告
================================================
评测问题数 : 3
平均相关性 : 0.468
平均忠实度 : 1.000
综合平均分 : 0.780
质量通过率 : 1.000
------------------------------------------------
✅ [0.62/1.00] 什么是向量数据库
✅ [0.22/1.00] RAG的作用是什么
✅ [0.56/1.00] 余弦相似度如何计算
================================================
```

每个 `✅` 是一个高质量的问题；`❌` 表示质量门未通过（综合分低于 0.6）。

### 3. 核心接口

```python
class RagEvaluator:
    def __init__(self, pipeline, answerer, threshold=0.6):
        ...
    def evaluate(self, golden, top_k=2):
        """对每对 (query, context) 打分。
        返回 {"summary": {...}, "rows": [...]}。"""

    @staticmethod
    def summary_report(evaluation):
        """格式化成人类可读的报告字符串。"""
```

`golden` 是测试集，每个元素是 `(query, ground_truth_context)` 元组。

---

## 端到端评分流程

```
query → retrieve → [context] → answer → ground
   → rel + faith → 综合分 → 质量门
```

每个问题的综合分：

```python
rel = answer_relevance(answer, question)
faith = grounding_score(answer, context)
score = weight_rel * rel + weight_faith * faith   # 默认各 0.5
passed = score >= threshold                        # 默认 0.6
```

综合平均分是所有问题的平均分；质量通过率是 passed 的比例。

---

## 端到端评估测试（9 用例全绿）

`test_rag_report.py` 覆盖了从单条报告到质量门判定的各个方面。

### 1. 报告内容

```python
def test_report_has_summary_header():
    """① 报告包含标题。"""
    report = RagEvaluator.summary_report(ev.evaluate(mk))
    assert "RAG 评估" in report
    assert "平均相关性" in report
    assert "平均忠实度" in report

def test_report_has_query_section():
    """② 报告有"评估明细"小节。"""
    report = RagEvaluator.summary_report(ev.evaluate(mk))
    assert "评估明细" in report

def test_each_query_shows_marks():
    """③ 每个 query 都有 ✅ 标记。"""
    report = RagEvaluator.summary_report(ev.evaluate(mk))
    for query in QUERIES:
        assert query in report
    assert report.count("✅") == 3
```

### 2. 单条报告

```python
def test_single_query_report():
    """④ 单条 golden 也能出报告。"""
    single = [{"q": "什么是向量数据库", "c": DOC}]
    res = ev.evaluate(single, top_k=2)
    assert len(res["rows"]) == 1
    report = RagEvaluator.summary_report(res)
    assert "什么是向量数据库" in report
```

### 3. 质量门判定

```python
def test_pass_rate_is_float():
    """⑤ 通过率是 0~1 的浮点数。"""
    res = ev.evaluate(mk, top_k=2)
    pr = res["summary"]["pass_rate"]
    assert isinstance(pr, float)
    assert 0.0 <= pr <= 1.0

def test_higher_threshold_stricter():
    """⑥ 阈值越高 → 通过率越低或持平。"""
    r_low = RagEvaluator(pipe, answerer, threshold=0.5).evaluate(mk)
    r_high = RagEvaluator(pipe, answerer, threshold=0.8).evaluate(mk)
    assert r_high["summary"]["pass_rate"] <= r_low["summary"]["pass_rate"]
```

### 4. 质量门逻辑

```python
def test_passes_gate_on_high_score():
    """⑦ 高分应过质量门。"""
    from quality import composite_score
    _, passed = composite_score("向量数据库是存储文本的向量系统", "向量数据库", DOC, 0.6)
    assert passed is True

def test_fails_gate_on_low_score():
    """⑧ 低分应过不了质量门。"""
    from quality import composite_score
    _, passed = composite_score("圆周率约等于3.14159黄金比例", "向量数据库", DOC, 0.6)
    assert passed is False
```

### 5. 汇总指标

```python
def test_summary_has_four_metrics():
    """⑨ 汇总含四个平均指标。"""
    res = ev.evaluate(mk, top_k=2)
    s = res["summary"]
    assert "avg_relevance" in s
    assert "avg_faithfulness" in s
    assert "avg_score" in s
    assert "pass_rate" in s

def test_summary_values_in_range():
    """⑩ 所有汇总指标在 0~1 之间。"""
    res = ev.evaluate(mk, top_k=2)
    for k, v in res["summary"].items():
        if k != "total":
            assert 0.0 <= v <= 1.0
```

---

## 测试工具类

为了测质量门逻辑，不依赖真实检索，构造了确定性检索器：

```python
class MockRetriever:
    """确定性检索器：返回固定上下文。"""
    def retrieve(self, query, top_k=2):
        return [DOC] * top_k


class MockAnswerer:
    """确定性答案器：答案来自上下文。"""
    def answer(self, question, contexts):
        return contexts[0] if contexts else ""
```

测试集构造：

```python
QUERIES = ["什么是向量数据库", "RAG 的作用是什么", "余弦相似度如何计算"]

def mk(n=3):
    return [{"q": q, "c": DOC} for q in QUERIES[:n]]
```

---

## 运行测试

```bash
cd ~/ai-testing-portfolio/rag-demo
pytest test_rag_report.py -v
# 期望: 9 passed
```

全量测试（含 D1-D5 检索质量、幻觉检测、答案质量）共 53 用例全绿：

```bash
pytest -q
# 期望: 53 passed
```

---

## 踩坑记录（重要）

| # | 踩坑 | 根因 | 修复 |
|---|---|---|---|
| 1 | `np.mean([])` 返回 NaN | 空 golden 集时 `np.mean` 无有效元素 | `evaluate` 空行时返回 `total=1`，汇总指标兜底 0.0 |
| 2 | `CompositeAnswerer` 导入报错 | D5 已实现的是 `StaticAnswerer`，测试里导错了名字 | 改测 `from quality import StaticAnswerer` |
| 3 | 报告里 query 没显示 `✅` | 报告没正确标记质量门通过状态 | `summary_report` 对每个高分 query 加 `✅` 标记 |

---

## 关键结论

- 端到端报告成功整合了检索质量、幻觉检测、答案质量三条线
- 综合分 = 相关性×w + 忠实度×(1-w)，质量门阈值 0.6
- 确定性实现，不依赖真实模型

---

## 待办

- [ ] 接入真实 embedding 服务验证报告准确性
- [ ] 报告支持更丰富的输出（JSON、表格）
- [ ] 添加 golden 数据集的管理工具

---

## 关联资料

- W1-D1：Pytest 基础
- W1-D2：参数化
- W1-D3：Mocking & Fixtures
- W2-D1：认识 LLM API
- W2-D2：参数化批量调用与响应校验
- W3-D1：RAG 入门
- W3-D2：接入真实 Embedder 与向量库
- W3-D3：检索质量评估
- W3-D4：端到端 RAG 问答与幻觉检测
- W3-D5：端到端答案质量打分与质量门
