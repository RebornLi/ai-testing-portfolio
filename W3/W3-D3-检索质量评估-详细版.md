# W3-D3 详细学习内容 · 检索质量评估（6-8 小时版）

> 日期：2026-09-06（周日）｜ 目标：评估 RAG 检索质量（召回/精准/MRR/命中率），用 golden 集
> 验收：`test_rag_eval.py` 9 用例全绿 + 学习日志 + commit

---

## ⏰ 今日时间块（6-8 小时）

| 时间段 | 时长 | 内容 |
|---|---|---|
| 09:00-10:30 | 1.5h | 为什么检索质量要量化（不能只看"能不能答"） |
| 10:30-12:00 | 1.5h | 四个指标：Recall@K / Precision@K / MRR / HitRate |
| 14:00-16:00 | 2h | 实战：eval 引擎（黑盒检索器 + golden 集） |
| 16:00-17:30 | 1.5h | 端到端质量门（关键词检索器 + 阈值卡质量） |
| 19:00-20:30 | 1.5h | 评估 vs RAGAS（今天只做检索维，faithfulness 留 D5） |
| 20:30-21:00 | 0.5h | 学习日志 + commit 打卡 |

---

## 一、为什么检索质量要量化（1.5h）★ 今日重点

> RAG 回答好不好，**一半看检索有没有找对资料**。光说"答得还行"不够——要能**量**出来才能迭代。
>
> 关键洞察：**"检索质量"和"回答质量"是两件事。**
> - 检索质量：相关文档有没有被找出来？
> - 回答质量：模型有没有用好找来的资料？
>
> 今天只做**第一件事**（检索质量）。回答质量幻觉检测留到 D4，完整 RAGAS 留到 D5。

> ⚠️ **认知边界**：真实检索质量需要 embedding 后的语义相似或 LLM judge。
> 今天用**确定性 golden + 关键词重叠**做等价替身，离线可跑，用来学"如何评估"方法本身。

---

## 二、四个指标（1.5h）

> 假设：检索返回若干 `id`，其中"该命中"的集合 = **golden**。

### 2.1 Recall@K
```
Recall@K = top-K 中命中 golden 个数 ÷ golden 总数
```
> 分母是 golden 数，不是 K。关心"该找到的找全了吗"。

### 2.2 Precision@K
```
Precision@K = top-K 中 golden 个数 ÷ K
```
> 关心"找出来的里有多少是真的"。

### 2.3 MRR（平均倒数排名）
```
MRR = 首个 golden 出现位置的倒数
```
> 第 2 → 1/2；第 3 → 1/3；没出现 → 0。

### 2.4 HitRate（命中率）
```
HitRate = top-K 里是否"至少命中一个 golden"？是=1，否=0
```

---

## 三、实战：指标函数（2h）★ 今日产出①

```python
"""eval_metrics.py — RAG 检索质量指标（纯函数）"""

def recall_at_k(hits, golden_ids, k):
    """Recall@k = top-k 中命中 golden 比例（分母=golden 数）。"""
    top = hits[:k]
    hit = [i for i in top if i in set(golden_ids)]
    if not golden_ids:
        return 0.0
    return len(hit) / len(golden_ids)


def precision_at_k(hits, golden_ids, k):
    top = hits[:k]
    hit = [i for i in top if i in set(golden_ids)]
    if not top:
        return 0.0
    return len(hit) / len(top)


def mrr(hits, golden_ids):
    """MRR = 首个 golden 位置倒数；未命中 → 0。"""
    for i, h in enumerate(hits):
        if h in set(golden_ids):
            return 1.0 / (i + 1)
    return 0.0


def hit_rate(hits, golden_ids):
    """HitRate = 是否至少命中一个 golden（0/1）。"""
    return 1.0 if any(i in set(golden_ids) for i in hits) else 0.0
```

---

## 四、实战：eval 引擎（1.5h）★ 产出②

```python
"""eval_engine.py — 用 golden 集评估检索质量"""

def evaluate(retriever, golden_queries, k=3):
    """返回 {"mean": {指标}, "per_query": [明细]}。

    retriever: 带 .retrieve(query, k) -> [(id, doc, score), ...]
    golden_queries: list[(query, set(golden_ids))]
    """
    per = []
    for query, golden in golden_queries:
        hits = [i for i, _, _ in retriever.retrieve(query, k)]
        per.append({
            "query": query,
            "recall@%d" % k: recall_at_k(hits, golden, k),
            "precision@%d" % k: precision_at_k(hits, golden, k),
            "mrr": mrr(hits, golden),
            "hit_rate": hit_rate(hits, golden),
        })
    n = len(per) or 1
    mean = {
        "recall@%d" % k: sum(p["recall@%d" % k] for p in per) / n,
        "precision@%d" % k: sum(p["precision@%d" % k] for p in per) / n,
        "mrr": sum(p["mrr"] for p in per) / n,
        "hit_rate": sum(p["hit_rate"] for p in per) / n,
    }
    return {"mean": mean, "per_query": per}
```

---

## 五、集成测试（产出③）★ 9 用例全绿

```python
"""test_rag_eval.py — 9 用例"""

class FakeRetriever:
    """返回预设顺序 id 的黑盒检索器。"""
    def __init__(self, order):
        self.order = order  # dict[query] = id 列表
    def retrieve(self, query, k=3):
        return [(i, f"doc-{i}", i) for i in self.order.get(query, [])[:k]]

def test_recall_at_k():
    """① 分母是 golden 数。"""
    r = FakeRetriever({"q": [10, 20, 30]})
    hits = [i for i, _, _ in r.retrieve("q", 3)]
    assert recall_at_k(hits, {20}, 1) == 0.0          # top1=10 没命中 20
    assert recall_at_k(hits, {20, 30}, 3) == pytest.approx(1.0)
    assert recall_at_k(hits, {20, 30, 99}, 3) == pytest.approx(2 / 3)

def test_precision_at_k():
    """② 占比。"""
    r = FakeRetriever({"q": [10, 20, 30]})
    hits = [i for i, _, _ in r.retrieve("q", 3)]
    assert precision_at_k(hits, {20}, 3) == pytest.approx(1 / 3)
    assert precision_at_k(hits, {20, 30}, 3) == pytest.approx(2 / 3)

def test_mrr():
    """③ 位置倒数。"""
    r = FakeRetriever({"q": [10, 20, 30]})
    hits = [i for i, _, _ in r.retrieve("q", 3)]
    assert mrr(hits, {20}) == pytest.approx(1 / 2)
    assert mrr(hits, {30}) == pytest.approx(1 / 3)

def test_hit_rate():
    """④ 0/1。"""
    r = FakeRetriever({"q": [10, 20, 30]})
    hits = [i for i, _, _ in r.retrieve("q", 3)]
    assert hit_rate(hits, {20}) == 1.0
    assert hit_rate(hits, {99}) == 0.0

def test_evaluate_aggregates():
    """⑤ evaluate 返回均值 + 明细。"""
    r = FakeRetriever({"q1": [5, 1, 2], "q2": [1, 5, 2]})
    res = evaluate(r, [("q1", {1}), ("q2", {1})], k=3)
    assert res["mean"]["recall@3"] == pytest.approx(1.0)
    assert res["mean"]["hit_rate"] == pytest.approx(1.0)
    assert len(res["per_query"]) == 2

def test_evaluate_empty_query_set():
    """⑥ 空 golden 集安全归 0。"""
    r = FakeRetriever({"q": [1, 2]})
    res = evaluate(r, [("q", set())], k=3)
    assert res["mean"]["recall@3"] == 0.0
    assert res["mean"]["precision@3"] == 0.0

import re

class KeywordRetriever:
    """按关键词重叠度检索，确定性。"""
    def __init__(self, docs):
        self.docs = docs
        self.kw = [self._kw(d) for d in docs]
    @staticmethod
    def _kw(text):
        text = re.sub(r'[。！？；，、,.!?\s]+', ' ', text)
        return set(ch for ch in text if ch.isalnum())
    def retrieve(self, query, k=3):
        scored = [(i, self.docs[i], len(self._kw(query) & self.kw[i]))
                  for i in range(len(self.docs))]
        scored.sort(key=lambda x: x[2], reverse=True)
        return scored[:k]

def test_quality_high_recall():
    """⑦ 高质量检索命中相关文档。"""
    docs = ["向量数据库存储和检索文本向量表示",
            "今天天气晴朗适合出门散步",
            "RAG 检索增强生成利用外部文档回答问题"]
    res = evaluate(KeywordRetriever(docs),
                   [("什么是向量数据库", {0}), ("RAG 是什么", {2})], k=1)
    assert res["mean"]["hit_rate"] == 1.0
    assert res["mean"]["recall@1"] == 1.0

class ConstantRetriever:
    """永远只返回 id=0 的检索器。"""
    def retrieve(self, query, k=3):
        return [(0, "doc-0", 1.0)]

def test_quality_low_recall_flagged():
    """⑧ 低质量检索应被质量门标记。"""
    res = evaluate(ConstantRetriever(), [("q", {1})], k=3)
    assert res["mean"]["recall@3"] < 0.5

def test_quality_threshold_gate():
    """⑨ evaluate 暴露分数，调用方可据此卡阈值。"""
    res = evaluate(ConstantRetriever(), [("q", {1})], k=3)
    score = res["mean"]["recall@3"]
    assert 0.0 <= score <= 1.0
    assert bool(score < 0.5) is True
```

---

## 六、失败自测（1h）

```python
def test_intentional_fail():
    r = FakeRetriever({"q": [1, 2, 3]})
    hits = [i for i, _, _ in r.retrieve("q", 3)]
    assert recall_at_k(hits, {2}, 1) == pytest.approx(1.0)  # 应为 0.0
```

---

## 七、运行 & 验证

```bash
cd ~/ai-testing-portfolio/rag-demo
pytest test_rag_eval.py -v
# 期望: 9 passed
```

---

## 八、学习日志模板（20:30）

复制 `~/ai-testing-portfolio/learning-log/2026-09-06.md`，填写：
- 今日学了什么：Recall/Precision/MRR/HitRate + golden 集评估
- 卡点：召回分母是 golden 数还是 K？（是 golden 数）
- 明日预习：W3-D4（端到端 RAG 问答 + 幻觉检测）

## 九、commit 打卡（21:00）

```bash
cd ~/ai-testing-portfolio
git add -A
git commit -m "W3D3: RAG 检索质量评估（Recall/Precision/MRR/HitRate）"
git push
```

---

## 📌 今日自检清单

- [ ] 能解释四个指标区别
- [ ] 召回分母是 golden 数
- [ ] 会写黑盒检索器 + golden 集评测
- [ ] 写了质量门
- [ ] 日志 + commit 完成

## 🆘 卡住怎么办

- 召回算不对 → 检查分母是 golden 数，不是 top-K 长度
- 想测真实检索 → 实现带 `.retrieve(query, k)` 的对象喂进 evaluate
- 卡 > 30min → 想清楚"golden 是标准答案文档"
- 卡 > 2h → 问木木，带报错信息

---

## 🎯 今日关键词

```
recall@k   → 该找到的找全了吗
precision@k → 找得干不干净
mrr        → 第一个答案排第几
hit_rate   → 命中没命中
golden集   → "标准答案"文档 id
质量门     → 分数卡质量
```

> 没有度量，就没有改进。先能"量"出检索质量，D4 才知道怎么优化。
