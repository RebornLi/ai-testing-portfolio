pytest
环境
Ubuntu 24
安装
1. 先确认系统依赖
sudo apt update
sudo apt install python3 python3-pip python3-venv

2. 创建项目虚拟环境
# 创建虚拟环境，文件夹名字 .venv
python3 -m venv .venv(文件名可以改)
# 激活虚拟环境
source .venv/bin/activate
#进入虚拟测试环境
source ~/w1d1/bin/activate

3.安装
sudo apt install pytest


