# W2-D5 详细学习内容 · 重试与限流（6-8 小时版）

> 日期：2026-09-01（周二）｜ 目标：给 LLM 调用加"重试 + 限流"保护，用 mock 429 验证重试逻辑
> 验收：`test_retry.py` 覆盖"429后成功 / 全失败 / 首成功 / 400立即抛"全绿 + 学习日志 + commit

---

## ⏰ 今日时间块（6-8 小时）

| 时间段 | 时长 | 内容 |
|---|---|---|
| 09:00-10:30 | 1.5h | 为什么 LLM 调用要重试（脆弱点 | 何时重试） |
| 10:30-12:00 | 1.5h | 指数退避算法 + 本机限流实测 |
| 14:00-16:00 | 2h | 实战：写 retry 客户端（monkeypatch 模拟 429） |
| 16:00-17:30 | 1.5h | 限流：客户端侧节流（rate limiting） |
| 19:00-20:30 | 1.5h | 全失败、边界、异常断言 |
| 20:30-21:00 | 0.5h | 学习日志 + commit 打卡 |

---

## 一、为什么 LLM 调用要重试（1.5h）★ 今日重点

> 调用 LLM API 是"网络操作"，网络就不稳定。常见失败：
> - **429 Too Many Requests**：触发限流（最常见）
> - **503 Service Unavailable**：服务端过载
> - **5xx**：服务器临时故障
> - **网络中断/超时**：连接断了

> 重试的价值：**限流是临时的，退后几秒重发往往就成功了。**
> 但**不能无限重试**（会压垮服务、会死循环），所以要有：
> 1. **最大重试次数**（硬上限，默认 3）
> 2. **退避等待**（等一会儿再试，通常是指数增长）
> 3. **只重试"可重试的状态码"**（429/503，不是 400）

### 1.1 关键决策：什么时候重试？
| HTTP 码 | 是否重试 | 原因 |
|---|---|---|
| 400 | ❌ 否 | 请求本身错了，重试也没用 |
| 401/403 | ❌ 否 | 认证/权限问题，重试没用 |
| 404 | ❌ 否 | 路径不存在 |
| **429** | ✅ 是 | 限流，退避后重试 |
| **5xx** | ✅ 是 | 服务过载，退避后重试 |

> **铁律：只有"重试可能有用"才重试。** 400/401/404 立即报错，别浪费。

---

## 二、指数退避 + 本机限流实测（1.5h）

### 2.1 什么是指数退避
> 每次失败等的时间越来越长：`0.1s → 0.2s → 0.4s → 0.8s …`
> 公式：`delay = base_delay × backoff^(失败次数)`，并设上限 cap。

```
第1次失败：等 0.1 × 2^0 = 0.1s
第2次失败：等 0.1 × 2^1 = 0.2s
第3次失败：等 0.1 × 2^2 = 0.4s  （超过 cap=5 就停在 5s）
```

> 好处：给过载的服务"喘息"时间，避免刚恢复就再次打爆。

### 2.2 本机限流实测
```bash
# 本机 vLLM 正常时不触发 429，但限流逻辑要测
# 可用 monkeypatch 伪造 429（下面实战部分）
echo "本机服务稳定，限流靠 mock 测"
```

> 真实调用时若要观察限流，可提高频率，或用 mock 伪造。

---

## 三、实战：写 retry 客户端（2h）★ 今日产出①

### 3.1 安装（venv）
```bash
pip install requests pytest
```

### 3.2 写 `test_retry.py`
```python
"""W2-D5 产出①：LLM 调用重试 + 限流测试（monkeypatch 模拟限流）"""
import os, time
import pytest
import requests

BASE_URL = "http://127.0.0.1:8000"
API_KEY = os.environ.get("LLM_API_KEY", "")


def chat_with_retry(prompt, max_retries=3, base_delay=0.1,
                    backoff=2.0, retry_statuses=(429, 503),
                    cap=5, sleep=None, calls=None):
    """带指数退避的重试客户端。

    - call_fn(prompt) -> requests.Response 或 {status, body}
    - 命中可重试状态码 → 退避重试
    - 超过 max_retries → 抛 RuntimeError

    返回 (content, attempts)。sleep/calls 用于测试注入。
    """
    if sleep is None:
        sleep = time.sleep
    if calls is None:
        calls = []
    last_status = 0
    for attempt in range(1, max_retries + 1):
        calls.append(attempt)               # 记录每次调用
        resp = chat(prompt)                 # 底层调用（可被 monkeypatch 替换）
        status = getattr(resp, "status_code", None)
        if status is None:                  # 兼容 dict 形式（mock 用）
            status = resp["status"]
        last_status = status
        if status is not None and 200 <= status < 300:
            content = getattr(resp, "json", lambda: {})()
            if not content:
                content = resp.get("body", "") if isinstance(resp, dict) else ""
            return content, attempt
        if status not in retry_statuses or attempt >= max_retries:
            break
        delay = min(base_delay * (backoff ** (attempt - 1)), cap)
        sleep(delay)
    raise RuntimeError(f"重试 {max_retries} 次仍失败，HTTP {last_status}")


def chat(prompt):
    payload = {
        "model": "ornith1.5-35b",
        "messages": [{"role": "user", "content": prompt}],
        "max_tokens": 50,
    }
    headers = {"Authorization": f"Bearer {API_KEY}"} if API_KEY else {}
    return requests.post(f"{BASE_URL}/v1/chat/completions",
                         json=payload, headers=headers, timeout=10)
```

> 注意：这个 `chat_with_retry` 不依赖本机是否在跑，因为测试用 monkeypatch 替换 `chat`。

### 3.3 用 monkeypatch 伪造限流（核心测试技巧）
```python
import requests as _requests

@pytest.fixture
def mock_limited(monkeypatch):
    """伪造一个前 2 次返回 429、第 3 次成功的聊天函数。"""
    state = {"n": 0}

    def fake_chat(prompt):
        state["n"] += 1
        fake = types.SimpleNamespace()
        if state["n"] <= 2:
            fake.status_code = 429
            fake.json = lambda: {"error": "rate limited"}
        else:
            fake.status_code = 200
            fake.json = lambda: {"choices": [{"message": {"content": "ok"}}]}
        return fake

    monkeypatch.setattr("test_retry.chat", fake_chat)
    return state


import types

def test_retry_succeeds_after_429(mock_limited):
    """① 前两次 429，第三次成功 → 应重试 3 次才拿到答案。"""
    content, attempts = chat_with_retry("hi", sleep=lambda s: None)
    assert content == "ok"
    assert attempts == 3


def test_retry_all_fail_raises(monkeypatch):
    """② 连续 503 直到超过上限 → 抛 RuntimeError。"""
    calls = []
    def fake_chat(prompt):
        calls.append(1)
        return types.SimpleNamespace(status_code=503,
                                     json=lambda: {"error": "unavailable"})
    monkeypatch.setattr("test_retry.chat", fake_chat)
    with pytest.raises(RuntimeError):
        chat_with_retry("hi", sleep=lambda s: None, calls=calls)
    assert len(calls) == 3   # 严格重试 3 次


def test_retry_first_try_ok(monkeypatch):
    """③ 第一次就 200 → 不重试，1 次成功。"""
    def fake_chat(prompt):
        return types.SimpleNamespace(status_code=200,
                                     json=lambda: {"choices": [{"message": {"content": "first"}}]})
    monkeypatch.setattr("test_retry.chat", fake_chat)
    content, attempts = chat_with_retry("hi", sleep=lambda s: None)
    assert content == "first"
    assert attempts == 1


def test_retry_400_not_retried(monkeypatch):
    """④ 400 错误请求 → 不重试，立即抛（只调 1 次）。"""
    calls = []
    def fake_chat(prompt):
        calls.append(1)
        return types.SimpleNamespace(status_code=400,
                                     json=lambda: {"error": "bad request"})
    monkeypatch.setattr("test_retry.chat", fake_chat)
    with pytest.raises(RuntimeError):
        chat_with_retry("hi", sleep=lambda s: None, calls=calls)
    assert len(calls) == 1   # 400 不重试
```

> 4 个用例覆盖了：成功重试 / 全失败 / 首成功 / 不重试 四种路径。

---

## 四、限流：客户端侧节流（1.5h）

> 光会重试不够，还要**主动控制发送速度**，避免触发 429。这叫限流。

### 4.1 简单令牌桶 / 定时发送
```python
"""限流：控制发送频率，避免触发服务限流。"""

class RateLimiter:
    """最小间隔节流器。"""
    def __init__(self, interval_seconds):
        self.interval = interval_seconds
        self.last = None

    def wait(self):
        import time
        now = time.time()
        if self.last is not None:
            wait = self.interval - (now - self.last)
            if wait > 0:
                time.sleep(wait)
        self.last = time.time()

    def __call__(self):
        self.wait()


# 使用：每秒最多发一次
limiter = RateLimiter(1.0)
for prompt in prompts:
    limiter()           # 节流
    chat(prompt)
```

> 这是"限流"的极简实现，生产环境用 `ratelimiter` 库或 Redis 分布式限流。

### 4.2 带限流的重试（组合起来）
```python
def chat_with_limiter_and_retry(prompt, limiter, **kwargs):
    limiter()
    return chat_with_retry(prompt, **kwargs)
```

> 组合逻辑：**先节流 → 再调用 → 失败才重试（重试时也可以再节流）**。

---

## 五、异常 + 边界（1.5h）

```python
import time

def test_retry_backoff_delays(monkeypatch):
    """⑤ 重试时确实等了退避时间（不是瞬时重试）。"""
    delays = []
    def fake_chat(prompt):
        return types.SimpleNamespace(status_code=429,
                                     json=lambda: {"error": "rl"})
    monkeypatch.setattr("test_retry.chat", fake_chat)
    try:
        chat_with_retry("hi", base_delay=0.05, backoff=2.0,
                        sleep=lambda d: delays.append(d), max_retries=3)
    except RuntimeError:
        pass
    assert delays == [0.05, 0.10]  # 指数退避: 0.05 → 0.10


def test_retry_no_timeout_in_production(monkeypatch):
    """⑥ 生产级重试要带 timeout，避免无限卡死。"""
    calls = []
    def fake_chat(prompt):
        calls.append(1)
        return types.SimpleNamespace(status_code=429,
                                     json=lambda: {"error": "rl"})
    monkeypatch.setattr("test_retry.chat", fake_chat)
    # 设很短的 base_delay 让它快速抛
    with pytest.raises(RuntimeError):
        chat_with_retry("hi", base_delay=0.01, max_retries=5, sleep=lambda s: None)
    assert len(calls) == 5  # 5 次上限
```

---

## 六、失败自测（1h）

```python
import pytest
import types

def test_intentional_fail():
    # 把"429 应重试"写成"应重试 99 次"，看 pytest 报什么
    monkeypatch_obj = monkeypatch
    state = {"n": 0}
    def fake_chat(prompt):
        state["n"] += 1
        return types.SimpleNamespace(status_code=200,
                                     json=lambda: {"choices":[{"message":{"content":"ok"}}]})
    monkeypatch_obj.setattr("test_retry.chat", fake_chat)
    content, attempts = chat_with_retry("hi", sleep=lambda s: None)
    assert attempts == 99   # 实测是 1
```

---

## 七、运行 & 验证

```bash
cd ~/ai-testing-portfolio
pytest tests/test_retry.py -v
# 期望: ①~⑥ 全绿（全部 monkeypatch，不依赖服务）
```

> 注意：这一天的测试**完全不依赖本机服务是否在跑**，因为 `chat` 被 monkeypatch 替换了。这是测试设计的关键——把"依赖环境的"隔离掉。

---

## 八、学习日志模板（20:00）

复制 `~/ai-testing-portfolio/learning-log/2026-09-01.md`，填写：
- 今日学了什么：何时重试、指数退避、monkeypatch 伪造 429、限流
- 实测真实值：退避延迟 [0.1, 0.2]（base_delay 0.1, backoff 2）；全失败重试 3 次后抛
- 卡点：为什么 400 不重试？monkeypatch 怎么替换函数？
- 明日预习：W2 总结 + W3 RAG 入门

## 九、commit 打卡（21:00）

```bash
cd ~/ai-testing-portfolio
git add -A
git commit -m "W2D5: LLM 调用重试（指数退避）+ 限流测试"
git push
```

---

## 📌 今日自检清单

- [ ] 知道何时重试（429/503）、何时不重试（400/401/404）
- [ ] 理解指数退避：0.1s → 0.2s → 0.4s
- [ ] 会用 monkeypatch 伪造限流响应测重试
- [ ] 写了带最大次数上限的重试
- [ ] 写了客户端限流（RateLimiter）
- [ ] 日志 + commit 完成

## 🆘 卡住怎么办

- monkeypatch 报错 → 确认替换的是模块级可调用对象
- 测试依赖真实服务 → 说明 monkeypatch 没生效
- 429 重试次数不对 → 检查 `retry_statuses` 和 `max_retries`
- 卡 > 30min：看 pytest 文档 monkeypatch 篇
- 卡 > 2h：直接问我（木木），带报错信息

---

## 🎯 今日关键词

```
retry_statuses      → 只重试 429/503，不重试 400/401/404
max_retries         → 硬上限（默认 3），防死循环
backoff             → 指数退避：0.1 → 0.2 → 0.4
monkeypatch         → 伪造 429，离线测重试
RateLimiter         → 客户端主动节流，避免触发 429
timeout             → 每次调用带超时，防卡死
```

> 今天学的是"稳健性"：服务不稳定时，重试 + 限流让你的程序不崩、不卡、不压垮别人。

---

## 🎯 W2 全周总览

| 日 | 主题 | 核心工具 |
|---|---|---|
| D1 | 认识 LLM API | requests/httpx + /v1/models |
| D2 | 参数化批量 + 校验 | parametrize + 结构断言 |
| D3 | Function Calling | tool_calls + 两轮对话 |
| D4 | 流式响应 | SSE + httpx stream |
| D5 | 重试 + 限流 | monkeypatch + 退避 |

> 一周下来，你已经掌握了 LLM API 测试的基本功。下周 W3 进入 **RAG**（检索增强生成）——这是 AI 评测的核心技能之一。
