# W2-D7 详细学习内容 · 里程碑收尾 + 复盘（6-8 小时版）

> 日期：2026-09-03（周四）｜ 目标：写周里程碑 + 全部 push 到 GitHub，做本周收尾
> 验收：`milestones/W2-LLM-API-测试.md` 完成 + 一周 7 个 commit + 复盘完成

---

## ⏰ 今日时间块（6-8 小时）

| 时间段 | 时长 | 内容 |
|---|---|---|
| 09:00-11:00 | 2h | 写里程碑 W2-LLM-API-测试.md |
| 11:00-12:00 | 1h | git 操作：检查分支、清理、准备 push |
| 14:00-16:00 | 2h | 全部 push 到 GitHub（7+ 个 commit） |
| 16:00-17:00 | 1h | 复盘：完成度 + 卡点 + 下周预习 |
| 19:00-20:00 | 1h | 学习日志 + commit 打卡 |

---

## 一、写里程碑（2h）★ 今日产出①

### 1.1 里程碑结构（同 W1-D7）
```
# W2 里程碑（2026-08-28 ~ 2026-09-03）

## 主题
## 本周产出
- [ ] 已完成项
## 可演示成果
- 运行方式：
- 结果：
## 复盘
- 完成度
- 卡点
- 下周改进
```

### 1.2 实战：填写 W2-LLM-API-测试.md
```markdown
# W2 里程碑（2026-08-28 ~ 2026-09-03）

## 主题
LLM API 测试实战（Phase 3）—— 掌握 OpenAI 兼容接口的全套测试基本功

## 本周产出
- [ ] D1：认识 LLM API（响应结构、核心参数、vLLM 对接）
- [ ] D2：参数化批量调用 + 响应结构校验（parametrize）
- [ ] D3：Function Calling（工具调用 2 轮循环）
- [ ] D4：流式响应 SSE 测试（逐块读取 + TTFT）
- [ ] D5：重试与限流（monkeypatch 模拟 429 + 指数退避）
- [ ] D6：性能与成本度量（TTFT / 时延 / token / 成本预算）
- [ ] D7：里程碑 + 复盘 + 全部 push

## 可演示成果
- 运行方式：cd ~/ai-testing-portfolio && curl /health && pytest
- 结果：本机 vLLM（ornith1.5-35b）TTFT≈0.06-0.10s；非流式 total_tokens≈132

## 复盘
- 完成度：85%（D1-D6 笔记 + 代码全落地，D7 收尾）
- 卡点：thinking 模型流式时 content 为空，要取 reasoning 块
- 下周改进：先建项目代码骨架，再写笔记
```

### 1.3 里程碑要"可演示"
- 运行方式要写清楚
- 结果要有真实数据
- 复盘要诚实（完成度、卡点都要写）

---

## 二、Git 操作（1h）★ 重点

> commit 链是一周活跃记录，招候选人会看你的 GitHub。

### 2.1 提交历史
```bash
git log --oneline
# 期望看到 7 天的提交（D1~D7）
```

### 2.2 清理与准备
```bash
git status        # 看状态
git diff          # 看改动
git checkout main  # 切回主分支
```

### 2.3 推荐每天一个 commit（比一次性 commit 更好，显示活跃）
```bash
cd ~/ai-testing-portfolio
# 每天提交，格式：W2D<N>: <做了什么>
git commit -m "W2D1: 认识 LLM API + 首个 chat completion"
git commit -m "W2D2: 参数化批量调用 + 响应校验"
git commit -m "W2D3: Function Calling 2 轮循环"
git commit -m "W2D4: 流式 SSE 测试"
git commit -m "W2D5: 重试 + 限流"
git commit -m "W2D6: 性能与成本度量"
git commit -m "W2D7: 周里程碑 + 复盘 + push"
```

### 2.4 推送到 GitHub
```bash
git push origin main
```

> ⚠️ **D7 现状说明**：本周的 6 天笔记是文档形式。如果要让 GitHub 显示"一周 7 次活跃"，建议把每天的实际测试代码（chat.py / test_batch.py / test_tool_call.py / test_streaming.py / test_retry.py / test_performance.py 等）也真的写进仓库。这样 commit 链才有料。

---

## 三、复盘（1h）★ 重点

> 复盘 = 看本周哪里做得好，哪里要改进。

### 3.1 复盘三问
1. 完成度：本周目标完成了多少？（打勾）
2. 卡点：最难的地方是哪里？怎么解决的？
3. 下周改进：下周要补什么？

### 3.2 本周卡点复盘（基于实际）
- [ ] 学习停在笔记文档，测试代码（chat.py 等）还没真正写进仓库
- [ ] thinking 模型流式时 content 为空，要找对字段（reasoning）
- [ ] API key 提取要按 `models.providers.qwen-local.apiKey` 的路径
- [ ] 性能断言用"上限"（宽松），不是精确值

### 3.3 下周预习（W3 — RAG）
- [ ] RAG 是什么（检索增强生成：检索 → 增强 → 生成）
- [ ] 分块（chunking）、Embedding、向量库
- [ ] 检索质量如何测试

---

## 四、学习日志（1h）

复制 `~/ai-testing-portfolio/learning-log/2026-09-03.md`，填写：
- 本周学了什么：LLM API 全套测试
- 最大的收获
- 卡点复盘
- 下周预习

## 五、commit 打卡（21:00）

```bash
cd ~/ai-testing-portfolio
git add -A
git commit -m "W2D7: 周里程碑 + 复盘 + push"
git push origin main
```

---

## 📌 今日自检清单

- [ ] W2-LLM-API-测试.md 里程碑写完了
- [ ] 里程碑可演示（运行方式、结果都有）
- [ ] 本周 7 个 commit（D1~D7）
- [ ] 全部 push 成功
- [ ] 复盘诚实
- [ ] 日志写完了

## 🆘 卡住怎么办

- `git push` 报错 → 检查远程仓库地址 `git remote -v`
- 推不上去 → 检查 `git push --force`（谨慎使用）
- 卡 > 30min：问木木，带报错信息

---

## 🎯 本周总产出清单（对照 roadmap Phase 3）

| 日 | 主题 | 产出 | 状态 |
|---|---|---|---|
| D1 | 认识 LLM API | 首个 chat completion | 笔记 |
| D2 | 参数化批量 + 校验 | test_batch.py（parametrize） | 笔记 |
| D3 | Function Calling | test_tool_call.py（2 轮循环） | 笔记 |
| D4 | 流式响应 | test_streaming.py（SSE） | 笔记 |
| D5 | 重试 + 限流 | test_retry.py（monkeypatch） | 笔记 |
| D6 | 性能与成本 | test_performance.py（TTFT/成本） | 笔记 |
| D7 | 里程碑 + 复盘 | milestones/W2-*.md | 今天写 |

## 一句话记忆

```
里程碑 + commit + push
↓
复盘：完成度 + 卡点 + 下周预习
↓
GitHub 一周 7+ commits
```

> 本周掌握了 LLM API 测试基本功。下周 W3 进入 RAG —— AI 评测核心技能。

---

## ✅ 附：本周真实结论（可直接抄进复盘）

| 项目 | 本机实测结论（2026-09-03） |
|---|---|
| 服务地址 | vLLM `http://127.0.0.1:8000`，`/health`=200 |
| 模型名 | `ornith1.5-35b`（thinking 模型，不是计划里的 nemotron3） |
| API Key 路径 | `~/.openclaw/openclaw.json` → `models.providers.qwen-local.apiKey` |
| 响应结构 | `choices[0].message.content`；流式在 `delta.content` |
| 流式特征 | 思考模型 content 为空，reasoning 在 `delta.reasoning` |
| 失败响应 | 空 messages=400、非法参数=400、不存在模型=404、无 key=401 |
| 性能 | 流式 TTFT≈0.06-0.10s；非流式 total_tokens≈132 |

> ⚠️ 诚实提醒：本周笔记里的测试代码（chat.py、test_batch.py 等）目前还是"文档里的示例"，要真正跑起来需要在 `ai-testing-portfolio/jd-validator/` 下创建项目并落地这些文件。这是 W3 要补的方向。
